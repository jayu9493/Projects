# mymodule/setup.py

from setuptools import setup, find_packages

setup(
    name="mymodule",
    version="1.0.0",
    author="Your Name",
    description="A custom module with basic operations",
    packages=find_packages(),  # Automatically find packages in the directory
)
